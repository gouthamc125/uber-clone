apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uber-web-app.fullname" . }}-test-connection"
  labels:
    {{- include "uber-web-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uber-web-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
